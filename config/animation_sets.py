"""This is where the animation sets are configured as python dictionaries"""
from config import helpers, resource_paths

# Player animation sets.
PLAYER_MAGE_ANIMATION_SETS = helpers.build_animation_set(
    resource_paths.PLAYER_MAGE_ANIMATIONS
)

# Ambient animations

# Enemy Animations
